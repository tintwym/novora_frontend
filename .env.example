# Local dev: leave empty — Vite proxies /api and /auth to localhost:8081.
# Production (Vercel): leave EMPTY — same-origin /api + /auth rewrites in vercel.json.
# NEVER set VITE_API_BASE_URL to your Render URL (cross-origin breaks session cookies → 403).
# VITE_API_BASE_URL=
